// packages/db: Drizzle schema, migrations, tenant-scoped data access [arch §5, tech-arch §1.2].
// Every table carries org_id; all access goes through the tenant-scoped layer (Next up: later step).
export {};
